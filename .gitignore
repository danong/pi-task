# Dependencies
node_modules/

# Runtime data (extension writes these to the agent dir, not the repo)
results/
cache/

# pi project-local state (auto-generated; .pi/settings.json is tracked)
.pi/sessions/
.pi/npm/
.pi/git/

# codebase-map session artifact (regenerated per session at the repo root)
codebase-summary.json

# Editor/OS noise
*.tsbuildinfo
.DS_Store
# Per-project todo list (pi extension)
.pi/TODO.json
